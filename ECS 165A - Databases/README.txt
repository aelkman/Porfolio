The purpose of the project was to create a database schema for the xml files provided, and perform specific queries dependent on problems provided by the professor.

It uses a postgreSQL database, so to run must have a db set up on local system
